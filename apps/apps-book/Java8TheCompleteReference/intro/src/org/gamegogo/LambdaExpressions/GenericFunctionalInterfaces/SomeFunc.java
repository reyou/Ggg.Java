package org.gamegogo.LambdaExpressions.GenericFunctionalInterfaces;

// Use a generic functional interface with lambda expressions.
// A generic functional interface.
interface SomeFunc<T> {
    T func(T t);
}

